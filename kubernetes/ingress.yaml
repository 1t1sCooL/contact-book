apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: contact-book
  namespace: default
  annotations:
    kubernetes.io/ingress.class: "traefik"
spec:
  rules:
    - host: mmalabugin.ru
      http:
        paths:
          - path: /contact-book
            pathType: Prefix
            backend:
              service:
                name: contact-book
                port:
                  number: 80
  tls:
    - hosts:
        - mmalabugin.ru
      secretName: main-page-tls
