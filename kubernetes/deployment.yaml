apiVersion: apps/v1
kind: Deployment
metadata:
  name: contact-book-app
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: contact-book-app
  template:
    metadata:
      labels:
        app: contact-book-app
    spec:
      containers:
        - name: contact-book-app
          image: contact-book-app:latest
          ports:
            - containerPort: 80
          env:
            - name: NODE_ENV
              value: "production"
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 10
